public class Main {
    public static void main(String[] args) {
        Game newGame = new Game();
        newGame.startGame();
        newGame.gameContent();
    }

    // Tuan sau : Text Game with window
    // Tuan sau nua : Game with UI
}